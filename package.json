{
  "name": "helsinki-city-departure",
  "version": "0.1.0",
  "description": "An app that provides real-time departure information for public transport in Helsinki.",
  "private": true,
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1",
    "nodemon": "^2.0.4",
    "uuid": "^8.3.0"
  },
  "scripts": {
    "start": "cd client && npm run build && cd ../ && node index.js",
    "server": "nodemon index.js",
    "client": "npm start --prefix client",
    "client-install": "npm install --prefix client",
    "client-build": "npm run build --prefix client",
    "deploy": "cd client && npm run build && cd ../ && eb deploy",
    "server-start": "node index.js"
  }
}
